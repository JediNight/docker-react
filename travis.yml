sudo: required
language: generic
services: -docker

before_install:
  - docker build -t toksfawibe/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true toksfawibe/docker-react npm run test
